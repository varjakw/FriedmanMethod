
class Friedman:

    MINIMUM_KEY_LENGTH = 1
    MAXIMUM_KEY_LENGTH = 10
    # Change these to estimate different key lengths

    # Estimate length of the unknown key
    # Parameter: ciphertext
    # Parameter: IndexOfCoincidence (IOC )for English

    averageIOC = MAXIMUM_KEY_LENGTH + 1

        for i in range(MAXIMUM_KEY_LENGTH - MINIMUM_KEY_LENGTH):

